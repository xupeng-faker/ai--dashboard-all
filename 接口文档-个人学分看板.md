# AI School 看板个人数据总览接口文档

## 1. 简介
本接口用于 AI School 看板中展示个人学分总览数据，包括目标学分、当前学分、达成率及部门标杆对比信息。

## 2. 数据库设计

### 2.1 新增表：个人学分表 (t_personal_credit)

| 字段名 | 类型 | 说明 | 备注 |
| :--- | :--- | :--- | :--- |
| id | BIGINT | 主键 ID | 自增 |
| employee_number | VARCHAR(50) | 工号 | 唯一索引，同步自 t_employee_sync |
| last_name | VARCHAR(100) | 姓名 | 同步自 t_employee_sync |
| lowest_dept_number | VARCHAR(50) | 最小部门编号 | 索引，同步自 t_employee_sync |
| lowest_dept | VARCHAR(200) | 最小部门名称 | 同步自 t_employee_sync |
| target_credit | DECIMAL(10, 2) | 目标学分 | 选课总学分 |
| current_credit | DECIMAL(10, 2) | 当前学分 | 已学完课学分 |
| personal_credit_completion_rate | DECIMAL(5, 2) | 个人学分达成率 | (当前/目标)*100，保留2位小数 |
| dept_benchmark_completion_rate | DECIMAL(5, 2) | 部门最小标杆学分达成率 | 同部门最大达成率 |
| credit_completion_date | DATETIME | 学分达成日期 | 首次达成时更新，之后不再更新 |
| create_time | DATETIME | 创建时间 | |
| update_time | DATETIME | 更新时间 | |

## 3. 接口定义

### 3.1 获取个人学分概览

**接口地址**: `/api/personal-credit/overview`  
**请求方式**: `GET`  
**描述**: 获取当前登录用户的学分完成情况及部门标杆对比。

**请求参数**:
无（通过 Cookie/Token 获取当前登录用户工号）

**响应参数**:

| 参数名 | 类型 | 说明 | 示例 |
| :--- | :--- | :--- | :--- |
| code | String | 响应码 | "200" |
| msg | String | 响应消息 | "success" |
| data | Object | 数据对象 | |
| ├─ employeeNumber | String | 工号 | "123456" |
| ├─ lastName | String | 姓名 | "张三" |
| ├─ lowestDept | String | 最小部门名称 | "开发一部" |
| ├─ targetCredit | Double | 目标学分 | 100.00 |
| ├─ currentCredit | Double | 当前学分 | 89.50 |
| ├─ personalCompletionRate | Double | 个人达成率(%) | 89.50 |
| ├─ deptBenchmarkRate | Double | 部门标杆达成率(%) | 95.00 |
| ├─ completionDate | String | 达成日期 | "2023-10-01" (未达成为 null) |

**响应示例**:
```json
{
  "code": "200",
  "msg": "success",
  "data": {
    "employeeNumber": "000123",
    "lastName": "张三",
    "lowestDept": "AI研发部",
    "targetCredit": 50.00,
    "currentCredit": 45.50,
    "personalCompletionRate": 91.00,
    "deptBenchmarkRate": 98.50,
    "completionDate": null
  }
}
```

### 3.2 触发个人学分数据同步

**接口地址**: `/api/personal-credit/sync`  
**请求方式**: `POST`  
**描述**: 触发个人学分全量计算与同步任务（供外部定时任务平台调用）。

**请求参数**: 无

**响应参数**:

| 参数名 | 类型 | 说明 |
| :--- | :--- | :--- |
| code | String | 响应码 |
| msg | String | 响应消息 |
| data | String | 结果描述 |

**响应示例**:
```json
{
  "code": "200",
  "msg": "同步成功",
  "data": "Sync completed successfully"
}
```

## 4. 定时任务逻辑 (外部调用)

**调用方式**: 外部定时任务平台通过 POST 请求 `/api/personal-credit/sync` 触发。

**处理流程**:
1.  **检查更新**: 查询 `t_employee_sync` 表中 `period_id` 的最新更新时间。
2.  **获取用户**: 获取 `t_employee_sync` 中最新周期的所有用户数据。
3.  **计算个人学分**:
    *   遍历每个用户：
    *   **目标学分**: 根据用户 `fourthdeptcode` (四级部门编码) 关联 `dept_course_selections` 获取选课列表，累加课程学分。
    *   **当前学分**: 查询用户在 `t_micro_study_info_sync` / `t_mooc_study_info_sync` 中已通过 (`is_pass='1'`) 且在选课列表中的课程，累加学分。
    *   **达成率**: `(当前学分 / 目标学分) * 100`，保留两位小数。
    *   **达成日期**: 若 `current_credit >= target_credit` 且原 `credit_completion_date` 为空，则设置为当前时间；否则保持不变。
    *   保存/更新至 `t_personal_credit` 表。
4.  **计算部门标杆**:
    *   按 `lowest_dept_number` 分组。
    *   计算每组中 `personal_credit_completion_rate` 的最大值。
    *   将该最大值更新回同部门所有用户的 `dept_benchmark_completion_rate` 字段。
